#include <bits/stdc++.h>
// #include <iostream> 

using namespace std;

int main(){
    // #ifndef ONLINE_JUDGE
    //     freopen("input.txt", "r", stdin);
    //     freopen("output.txt", "w", stdout);
    // #endif

    // int a = 2;
    int a;
    a = 2;

    short int b = 23;
    long int c = 2873594;

    sizeof(a);

    // int d = 'A';
    char d = 'A';

    // cout << d;

    // int x = 3 / 2;
    // int x = double(3 / 2);
    // int x = double(3) / 2;
    // double x = double(3 / 2);
    // double x = double(3) / 2;
    double x = 3.0 / 2;

    // cout << x;

    long long y = 2 * 1LL;

    // cout << y;

    cout << 1e20 << endl;
    cout << fixed << 1e20 << endl;
    cout << fixed << setprecision(0) << 1e20; // setprecision(0) krne seh 0 seh decimal ke baad 0 digits ajati but fixed bhi likhna padega agr decimal ke baad dekhna nhi toh total digits dekhi jati h

    return 0;
}